/**
 * Constant PlayerState.
 * Stays consistent over all GameScreens.
 * This class only holds information about the player: e.g. HP, Weapons, Items, ...
 */
const PlayerState = {

};

export default PlayerState;